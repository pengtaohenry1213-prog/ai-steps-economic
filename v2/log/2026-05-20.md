# 2026-05-20

```base
构建成功！Plan 生成能力已整合到 strategy-core。

  完成总结

  新增文件：

  | 文件                                      | 说明
                      |
  |-----------------------------------------|-----------------------------------------------
  ----------------|
  | src/prompts/plan-generation-prompt.ts   | Plan 生成 Prompt（参考 01-planner.md 和
  plan-template.md）           |
  | src/services/plan-generation-service.ts | Plan 生成服务 generatePlanFromStep() + 格式化
  formatPlanAsMarkdown() |

  扩展类型：

  // types/index.ts
  export interface PlanTodo {
    id: string
    type: 'frontend' | 'backend' | 'test' | 'fix'
    content: string
    depends_on: string[]
    acceptance: string
  }

  export interface PlanDocument {
    stepNumber: number
    overview: string
    stagePhases: [...]
    keyRisks: [...]
    todos: PlanTodo[]
    files: [...]
    acceptance: [...]
  }

  导出：

  // index.ts
  export { generatePlanFromStep, formatPlanAsMarkdown } from
  './services/plan-generation-service'

  执行链路：

  StepN.md → [generatePlanFromStep] → PlanDocument → [formatPlanAsMarkdown] → stepN-plan.md

  现在 Web 层可以在生成 Step 文档后，调用 generatePlanFromStep() 生成对应的 Plan 文档。

```

⏺ Prompt 修改完成。以下是变更总结：

  ---
  修改内容

  1. Step 9 特殊拆分规则（新增）

## Step 9 特殊拆分规则（CI/CD 必须尽早接入）

  **Step 9 是集成测试与部署阶段，但 CI/CD 配置不应等到所有功能开发完成。**

  拆分要求：

- **Step 9a**：CI/CD 基础配置（在 step5 之后引入，尽早接入 CI）
- **Step 9b**：集成测试与部署（在 step8 之后，所有功能完成后执行）

  1. v1 复用率定义（新增）

## v1 复用率定义（禁止技术栈复用）

- **禁止**将"集成 HyperFormula"算作复用（这是技术集成，不是功能复用）
- 正确示例："复用 v1 的财务函数 XNPV/IRR 的单元测试用例"
- **违反后果**：Step 6 声称 v1复用率 60% 但实际是集成 HyperFormula，则违反复用率定义

  1. Out of Scope 强制要求（强化）

## Out of Scope 强制要求（每个 Step 必须包含）

- Out of Scope 不能为空，如果 AI 无法确定，列出"不支持未来可能需要的功能"作为占位
- **违反后果**：如果某个 step 缺少 Out of Scope 章节，或 Out of Scope 为空，则该 step
  不符合输出要求

  1. 重复内容清理

- 删除了重复的 "每个 Step 必须包含 Out of Scope" 章节（因为已在上面强化）
- 删除了重复的 "风险提示必须有应对方案" 章节（因为已在上面强化）

  ---
  下一步

  Shell 环境问题导致无法执行 npm run build。请手动验证：

  cd /Users/taopeng/workspace/AI_2026/AI-STEPS-ECONOMIC/v2/packages/strategy-core
  npm run build

  构建成功后，重新执行"生成项目文档包"即可看到优化后的 Steps。

  ---


改进计划                                                                   │
│                                                                            │
│ 1. 修改 steps-dev-generation-prompt.ts                                     │
│                                                                            │
│ 增加阶段内约束（阶段约束规则）                                             │
│ 在 JSON 输出格式中增加 phaseConstraints 字段，或在 prompt 中强化约束规则： │
│                                                                            │
│ ### 阶段内约束（每阶段必须遵守）                                           │
│ - 本阶段只实现当前 step 的功能                                             │
│ - 不在本阶段提前实现后续 step 的功能                                       │
│ - 遇到「建议」但不强制的内容，先记录到 TODO，留到下一阶段                  │
│ - Out of Scope 中明确标注的内容，不得在当前阶段实现                        │
│                                                                            │
│ 增加 Human Gate 列                                                         │
│ 在 progressTracking 的 JSON 结构中增加 humanGate 字段：                    │
│ "progressTracking": [                                                      │
│   {                                                                        │
│     "step": "step0",                                                       │
│     "status": "✅ 完成",                                                    │
│     "humanGate": "HG1 - 框架验收",                                         │
│     "role": "Backend",                                                     │
│     "planFile": "doc/plans/step0-plan.md",                                 │
│     "completedDate": "-"                                                   │
│   }                                                                        │
│ ]                                                                          │
│                                                                            │
│ 明确 Plan 文件路径结构                                                     │
│ 在 prompt 中补充：                                                         │
│ Plan 文件目录结构：                                                        │
│ .cursor/plans/                                                             │
│ ├── README.md           # Plan 使用说明                                    │
│ ├── plan-template.md    # Plan 模板                                        │
│ └── stepN-plan.md       # 每个 Step 的 Plan（如 step0-plan.md）            │
│                                                                            │
│ Plan 文件命名规则：step{N}-plan.md（如 step1-plan.md）                     │
│                                                                            │
│ 2. 修改 steps-dev-generation-service.ts                                    │
│                                                                            │
│ 更新 progressTracking 类型定义，增加 humanGate 字段。                      │
│                                                                            │
│ 3. 修改 step-generation-prompt.ts                                          │
│                                                                            │
│ 在 Step 生成 prompt 中强化 Out of Scope 与阶段约束的关联：                 │
│ - "当前 step 不在 Out of Scope 中的功能，不得提前到其他阶段实现" 

---

已完成集成方案A：

  1. 更新 step-split-service.ts
  - splitStepsIntoFiles 支持 options.projectName 参数
  - 返回值包含 filePath（如 my-project/doc/steps/step0.md）

  2. 添加依赖
  // apps/workflow-dashboard/package.json
  "dependencies": {
    "@ai-toolkit/strategy-core": "workspace:*",
    ...
  }

  前端使用方式（在 lifecycleStore.ts 的 executeInitialization 中）：

  import { splitStepsIntoFiles } from '@ai-toolkit/strategy-core'

  // 生成 steps 后，调用 splitStepsIntoFiles
  const stepDocs = steps || []  // StepDocument[]
  const { stepFiles } = splitStepsIntoFiles(stepDocs, {
    baseDir: 'doc/steps',
    projectName  // 例如 'my-project'
  })

  // 追加到 allFiles
  const allFiles = [
    ...generatedFiles.map(f => ({ ...f, path: `${projectName}/${f.path}` })),
    ...cursorRules.map(r => ({ path: `${projectName}/${r.path}`, content: r.content })),
    ...stepFiles.map(sf => ({ path: sf.filePath, content: sf.content }))  // 新增
  ]

  运行 pnpm install 后即可使用。请在 lifecycleStore.ts 的 executeInitialization 函数中实现上述逻辑。

---

？？？

1. 经过测试，有了一个新的问题，在点击“生成项目文档包”后，在生成各个阶段的文档时，会报错，分析原因是MiniMax的网络问题，会有中断，所以需要优化。如果出现中断，需要重试，重试3次后，如果还是失败，则提示用户，并记录日志。还是您有其他方案给我，麻烦。

2. "生成项目文档包"生成了一系列的文档，然后我用大steps拆分了一些小step，然后在Cursor IDE中以Plan/Agent方式执行这些小step，如果顺利和理想，整个项目就完成了。我有一个新的问题，如果这个项目后期有新的需求，比如新增一个功能，或者修改一个功能，我需要重新生成项目文档包，然后重新执行这些小step么？这样很麻烦。您有其他方案给我，麻烦。
