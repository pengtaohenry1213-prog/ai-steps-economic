---
alwaysApply: true
---

# 项目架构文档生成器

请基于以下项目工程规范文档，生成一份完整的【项目架构文档】，严格遵循所有要求。

## 输入参考文档（必须严格遵守其规则）

| 序号 | 规范文件 | 说明 |
|------|----------|------|
| 1 | `.cursor/prompts/run-step.md` | 执行状态追踪表、状态定义、Human Gate记录规范 |
| 2 | `.cursor/prompts/execute-plan.md` | Agent执行规则、日志格式、Git提交规范 |
| 3 | `.cursor/prompts/generate-plan.md` | Plan文件生成规则、输出路径要求 |
| 4 | `.cursor/prompts/human-gate.md` | Human Gate核心定义与执行标准 |
| 5 | `.cursor/prompts/step-template.md` | 单Step模板格式（7个必填字段） |
| 6 | `.cursor/prompts/steps-dev.md` | 通用开发阶段划分、依赖逻辑、角色分类 |
| 7 | `.cursor/prompts/pm-human-gate.md` | PM Human Gate检查项与决策格式 |
| 8 | `.cursor/prompts/security-human-gate.md` | Security Human Gate检查项与决策格式 |

## 文档核心要求

1. **架构部分**：先定义项目整体技术架构和工程化架构，分层明确职责
2. **Step拆分部分**（核心）：将项目全生命周期拆分为可独立执行的Step，每个Step严格遵循step-template.md的7字段格式
3. **执行规范部分**：补充项目统一的执行规则、Human Gate规则、进度追踪规则、版本控制规则
4. **验收闭环部分**：明确所有Step完成后的最终验收标准

## Step拆分强制要求

1. **阶段划分**：必须分为4个标准阶段，严格遵循steps-dev.md的依赖逻辑：
   - 第零阶段：项目初始化（工程化基础配置）
   - 第一阶段：基础搭建（前后端基础项目初始化，可并行）
   - 第二阶段：核心功能（后端API优先，再开发对应前端）
   - 第三阶段：集成优化（联调、部署、稳定性优化）
2. **每个Step必须包含**：
   - Step编号：StageX-StepXX格式
   - Step目标：仅聚焦当前Step的具体任务
   - 参与者：明确参与角色（PM/Dev/Tester/Arch/Sec/PMO）
   - 输入条件：明确前置依赖
   - 执行动作：按顺序列出可执行的操作，必须包含：
     - 代码编写/配置修改动作
     - 调试/验证命令
     - 测试用例执行命令
     - Human Gate执行时机（HG1/HG2）
     - Git提交命令
   - 输出物&验收标准：必须量化可验证，必须包含**实测命令和预期输出**
   - 关联依赖：明确前置和后续Step
   - 文件变更清单：表格形式，列出所有将修改/新增的文件
3. **必须严格遵循run-step.md的执行流程**：
   - 开发阶段Step执行前执行Human Gate 1
   - 测试/验收阶段Step执行后执行Human Gate 2
   - 每个Step必须包含实测测试要求，不允许只写"通过"
   - 每个Step完成后必须有明确的Git提交命令
4. **必须严格遵循tracking.md的规范**：
   - 明确每个Step的状态更新规则
   - 明确更新日志的记录要求
   - 明确阻塞问题的追踪方式

## 文档结构（严格按此顺序）

1. 文档概述：说明文档目的和适用范围
2. 核心架构体系：
   - 项目技术架构（分层表格，包含层级、核心组件、技术栈、职责）
   - 工程化架构（表格形式，包含模块、核心配置、工具、目标）
3. 项目全生命周期拆分步骤：
   - 第零阶段：项目初始化（工程化基础配置）
   - 第一阶段：基础搭建（后端基础→前端基础）
   - 第二阶段：核心功能（后端核心API→前端核心组件→集成验证）
   - 第三阶段：集成优化（部署优化→稳定性优化）
   （每个阶段下的每个Step严格使用step-template.md的表格格式）
4. 执行规范补充：
   - Plan文件生成规则（对应generate-plan.md）
   - Human Gate执行规则（对应human-gate.md、pm-human-gate.md、security-human-gate.md和run-step.md）
   - 进度追踪规则（对应run-step.md）
   - 版本控制规则（对应execute-plan.md）
5. 验收闭环：明确所有Step完成后的最终验收标准

## 项目专属参数（可根据实际项目替换）

### 技术栈配置

| 类别 | 技术选型 |
|------|----------|
| 前端框架 | Vue3 + TypeScript + Vite |
| 前端表格 | Luckysheet（在线协作表格） |
| 后端框架 | Node.js + Express/NestJS + TypeScript |
| 数据库 | PostgreSQL（Supabase） |
| 实时协作 | Supabase Realtime |
| 包管理 | pnpm workspace |
| 部署方式 | Docker + Docker Compose / Vercel |

### 核心功能模块

| 模块 | 说明 |
|------|------|
| 模型管理 | 模型创建、编辑、删除、列表展示 |
| 版本管理 | 版本状态流转（草稿→已提交→已锁定） |
| 表格编辑 | 树形表格、单元格编辑、虚拟滚动 |
| 公式计算 | XIRR/XNPV/NPV/IRR等财务函数 |
| Excel导入导出 | 兼容v1模板的批量数据处理 |
| 权限管理 | 集团用户/锁定者/其他用户三级权限 |

### 生命周期阶段定义

| 阶段 | 名称 | 管理位置 | 说明 |
|------|------|----------|------|
| init | 立项 | workflow-dashboard | 提案生成、可行性评估 |
| requirement | 需求 | workflow-dashboard | 需求补充、PRD |
| architecture | 架构 | workflow-dashboard | 系统架构设计 |
| initialization | 初始化 | 交接点 | 项目脚手架 + Cursor Rules |
| development | 开发 | Cursor | 前端组件、后端API开发 |
| testing | 测试 | Cursor | AI测试设计、单元测试 |
| acceptance | 验收 | Cursor | 验收报告、review |
| packaging | 打包 | Cursor | Dockerfile、部署脚本 |
| deployment | 部署 | Cursor | 多环境部署 |
| iteration | 迭代 | Cursor | 需求收集、优化 |

## 强制约束

1. 所有规则必须严格对齐输入的8份工程规范文档，不得自行修改
2. 每个Step必须可独立执行，粒度清晰，一个Step对应一个独立功能块
3. 所有验收标准必须可量化、可验证，必须包含实际可执行的命令
4. 必须明确每个Step的Human Gate执行时机和决策标准
5. 必须明确每个Step完成后的Git提交命令和更新tracking的要求
6. 不允许生成空泛的描述，所有内容必须可直接用于项目执行

## Human Gate 执行规范

### HG1 执行前审查（development ~ iteration 阶段执行）

**触发时机**：每个 development 阶段 Step 执行前

**检查项**：

| Gate | 检查项 | 说明 |
|------|--------|------|
| PMO | 需求完整性 | 验收标准、优先级、依赖逻辑 |
| PMO | 技术可行性 | 架构设计、接口定义 |
| Security | 安全规则匹配 | 无危险命令、无路径穿越 |
| Security | 敏感数据检查 | 无硬编码密钥、无明文传输 |

**决策格式**：

| 决策 | 含义 | 后续动作 |
|------|------|----------|
| **PASS** | 审查通过，可继续执行 | 进入执行阶段 |
| **CONDITIONAL** | 非核心项未达标 | 记录整改项，继续执行 |
| **REJECT** | 阻塞，必须修复 | 停止执行，等待人工签字确认 |

### HG2 执行后复审（testing ~ acceptance 阶段执行）

**触发时机**：每个 testing/acceptance 阶段 Step 执行后

**检查项**：

| Gate | 检查项 | 说明 |
|------|--------|------|
| PMO | 结果校验 | 验收标准逐项确认 |
| PMO | 日志校验 | 执行日志完整性 |
| Security | 敏感数据泄露 | 无PII数据暴露 |
| Security | 安全漏洞 | 无XSS/SQL注入等漏洞 |

**决策格式**：同上

### REJECT 硬性约束

- REJECT = 立即停止，不允许执行任何后续步骤
- 必须等待人工介入并签字确认
- 人工确认后，在Plan文件中记录决策信息

## 实测测试规范

**重要：不允许只填写"✓"或"通过"，必须有实际执行的命令和输出**

每个Step的测试报告必须包含：

| 测试项 | 内容要求 |
|--------|----------|
| 测试命令 | 实际可执行的curl/npm命令 |
| 预期结果 | 明确的HTTP状态码/返回值 |
| 实际结果 | 执行后的真实输出 |
| 测试状态 | PASS / FAIL |
| 测试时间 | 执行时间戳 |

**测试报告格式**：

```markdown
## 测试报告

| 用例 | 命令 | 预期结果 | 实际结果 | 状态 | 时间 |
|------|------|----------|----------|------|------|
| 登录接口 | curl -X POST ... | {"code":200} | {"code":200} | PASS | 10:30 |
```

## Step 模板格式

```markdown
#### step{N}.md - {功能名称}

| 字段 | 内容 |
|------|------|
| Step编号 | Stage{X}-Step{N} |
| Step目标 | {具体任务描述} |
| 输入条件 | {前置资源/数据/环境要求} |
| 执行动作 | 1. {动作1}<br>2. {动作2}<br>... |
| 输出物&验收标准 | 1. {可量化验收点}<br>2. {实测命令示例} |
| 关联依赖 | 前置：step{X}；后续：step{Y} |
| Human Gate | HG1 / HG2 |
| 文件变更清单 | | 文件 | 操作类型 | 说明 |
| |------|----------|------|
| | src/xxx.ts | 新增 | 功能说明 |
```

---

## 需求 → Step 转换流程

将业务需求文档（PRD）转换为标准化的 stepN.md 文件的完整流程。

### Step 1：需求分析

读取需求文档，提取以下关键信息：

| 信息类型 | 说明 | 示例 |
| --- | --- | --- |
| 背景 | 为什么要做这件事 | 用户需要批量导入数据 |
| 目标 | 要达成什么结果 | 支持 Excel 模板批量导入 |
| 用户场景 | 谁在什么情况下使用 | 财务人员在月初批量导入数据 |
| 功能范围 | 要做哪些功能 | Excel 解析、字段映射、数据校验 |
| 不做清单 | 明确不做的内容 | 不支持 CSV、不支持公式解析 |

### Step 2：Step 拆分

根据阶段划分原则，将需求拆分为多个 step：

| 阶段 | 适用场景 | Step 类型 |
| --- | --- | --- |
| 第零阶段 | 项目初始化、工程化配置 | step0 |
| 第一阶段 | 前后端基础项目初始化 | step1, step2 |
| 第二阶段 | 核心功能链路 | step3, step4, step5 |
| 第三阶段 | 集成与优化 | step6, step7 |

**拆分原则**：

- 一个 step = 一个独立可验收的单元
- step 之间尽量无依赖（或仅 minor 依赖）
- 每个 step 的工作量为 0.5-2 人天

### Step 3：生成 stepN.md

对照上述 Step 模板格式，为每个 step 生成标准化的 step 文件。

### Step 4：自检清单

生成 stepN.md 后，对照以下清单自检：

| 检查项 | 说明 | 必须 |
| --- | --- | --- |
| 目标明确 | 用一句话能说清楚 step 要完成什么 | ✅ |
| 输入条件完整 | 前置资源/数据/环境都已明确 | ✅ |
| 执行动作具体 | 每个动作都是可执行的 | ✅ |
| 验收标准可测 | 有明确的验证方式和量化指标 | ✅ |
| 不做清单明确 | 明确标注了 Out of Scope | ✅ |
| 依赖关系清晰 | 前置/后续 step 都已标注 | ✅ |
| 测试用例覆盖 | 核心路径有对应的测试用例 | ✅ |

### Step 5：Human Gate PM 评审

将生成的 stepN.md 提交 Human Gate PM 评审：

| 检查项 | 说明 |
| --- | --- |
| 需求完整性 | 背景与目标清晰 |
| 验收标准 | 功能验收标准可执行、可观测 |
| 优先级 | P0 主路径清晰 |
| 依赖与风险 | 外部依赖已识别 |

**决策**：

- **PASS** → 进入执行阶段
- **REJECT** → 修正后重新评审

### 输出规范

- `docs/steps/stepN.md`（每个拆分的 step 一个文件）
- 更新 tracking 表中的 step 状态

### 强制规则

1. stepN.md 必须通过 Human Gate PM 评审后才能执行
2. 不允许合并多个需求到一个 step
3. 验收标准必须量化（不可量化的标准需拆分）
4. 每个 step 必须有对应的测试用例
