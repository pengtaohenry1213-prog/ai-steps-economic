## 3. 表格列配置模块

### 3.1 `column/fixed.ts` - 固定列定义

**文件位置**: `views/instance/edit/utils/column/fixed.ts`

**完整模型固定列**:

```typescript
export const fixedColumns = [
  { title: '编码（导入需携带）', field: 'metricCode', visible: false },
  { title: '类别（导入需携带）', field: 'metricCategory', visible: false },
  { title: '是否单一值', field: 'isFixed', visible: false },
  { title: '单位编码', field: 'unitCode', visible: false },
  { title: '刻度', field: 'scale', visible: false },
  { title: '序号', field: 'sort', width: 50, fixed: 'left' },
  { title: '科目名称', field: 'metricName', width: 280, treeNode: true },
  { title: '单位', field: 'unit', width: 70, slots: { edit: 'unitEdit' } },
  { title: '单一值', field: 'isFixeds', width: 80 },
];
```

**Excel 导入列索引**:

| 索引 | 字段 |
|------|------|
| 0/A | metricCode |
| 1/B | metricCategory |
| 2/C | isFixed |
| 3/D | unitCode |
| 4/E | scale |
| 5/F | sort |
| 6/G | metricName |
| 7/H | unit |
| 8/I | isFixeds |
| 9+ | 日期列 (YYYY 或 YYYY-Q) |

---

### 3.2 `column/index.ts` - 动态列生成

**文件位置**: `views/instance/edit/utils/column/index.ts`

**核心函数**:

```typescript
export function initColumns({ forecastTimeRange, forecastTimeType }) {
  // 解析时间段: "2023-1,2027-12"
  const [startYear, startMonth] = range[0].split('-');
  const [endYear, endMonth] = range[1].split('-');
  
  // 年度列配置
  const yearColumns = [...fixedColumns, ...yearUnfixedColumns];
  
  // 季度列配置
  if (forecastTimeType === 'quarter') {
    // 生成季度树: [{ year: 2023, children: [1,2,3,4] }, ...]
    const { quarters, quartersTree } = generateQuarterRange(...);
  }
  
  // 预设值（A=历史, F=预测）
  const presets = {};
  dateFields.forEach(item => {
    presets[item] = isFuture(item) ? 'F' : 'A';
  });
  
  return { yearColumns, dateFields, quarterColumns, presets };
}
```

---