# 经济测算模型二期规划内容

## 二期规划升级内容清单（完整含解决方案）

| 升级内容 | 目的 | 负责人 | 周期 | 权重 | 解决方案 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 公式智能提示：输入 `${` 时自动弹出指标选择下拉框，支持搜索和模糊匹配。<br>公式语法高亮：对公式中的指标、常量、函数进行不同颜色高亮显示。<br>公式版本管理：保存公式历史版本，支持回滚和对比 | 降低用户学习成本，提升公式编写效率 | 彭涛 | 二期规划 |  | 公式选择、编辑使用CodeMirror6集成轻量公式编辑器，其支持：公式自然、提示输入和标签块显示+删除，集成自定义公式函数和单元格指标的插入。注：如果公式需要兼容 Excel 语法：换成 HyperFormula + CodeMirror |
| 实时协作：基于 WebSocket 或 Supabase-表realtime功能 实现多人同时编辑，实时同步变更。<br>锁机制：单元格/区域锁定，防止编辑冲突。<br>操作日志：记录所有操作，支持审计和回溯。<br>评论与标注：单元格可添加评论和标注，@相关人员。<br>任务分配：将模型不同部分分配给不同人员。 | 团队协作建模、评审模型时提出修改意见，提升沟通效率 | 彭涛 | 二期规划 | 90 | 参考共享文档，以 Vue3+Vite+vxe-table 为前端渲染层，Node.js/Java+ws 搭建 WebSocket 实时服务，参考 Luckysheet「连接池-数据更新-广播」流程，实现多用户实时同步；通过服务端中心化锁管理防冲突，全量操作日志做审计回溯，单元格插槽扩展实现评论/标注/任务分配。<br>**技术栈**<br>前端：Vue3.4+、Vite5、vxe-table@4.9.31、vxe-pc-ui、Pinia、原生WebSocket/Supabase<br>后端：Node.js/Java、ws服务/Supabase表realtime、lowdb/JSON文件持久化 |
| 错误定位：快速定位计算错误的具体单元格。<br>错误传播：显示错误如何传播到其他单元格。<br>建议修复：根据错误类型提供修复建议。<br>调试模式：逐步执行公式，显示每一步计算结果和中间变量。 | 在每个单元格附加「计算过程」功能，提供更好的错误诊断能力 | 彭涛 | 二期规划 | 85 | 错误定位&传播：采用Floyd快慢指针算法计算闭环；错误类型分为闭环、无效指标、无效公式、孤立指标；无效公式包含三类原因：ID不存在、未添加ID、未纳入拓扑排序图。<br>调试模式：复用原有前端调试能力，提取`validateSortedGraphWithFormula`功能展示中间过程。<br>闭环兼容：参考Excel迭代计算原理，循环多轮逼近近似结果，满足财务闭环计算场景，区别于标准Kahn拓扑有序计算 |
| 资源监控：监控公式、表格、计算引擎、数据库等资源使用情况。<br>健康检查：围绕数据完整性、公式正确性及依赖关系，做全量校验与异常检测。 | 系统监控，保障系统稳定运行 | 彭涛 | 二期规划 | 80 | **资源监控（resource-monitor.ts）**<br>公式监控：数量、复杂度、依赖链深度；表格监控：行列数、可见范围、数据量；复用`dependencies.ts`/`useData.ts`/`usePageData.ts`/`useChangeData.ts`现有统计模块。<br>**健康检查（health-check.ts）**<br>循环依赖检测、公式语法验证、无效引用检测、孤立指标分析、数据完整性校验；复用`cycle.ts`/`dependencies.ts`/`calculate.ts`/`useData.ts`已有能力 |
| 自动备份：定时自动备份数据。<br>手动备份：支持手动触发备份。<br>增量备份：只备份变更数据。<br>一键恢复：快速恢复到指定备份点。<br>跨环境迁移：支持数据在不同环境间迁移。 | 数据安全保障 | 彭涛 | 二期规划 | 110 | 1.手动备份：点击保存触发，命名规则「[手动]版本名 日期时间」，接口落地服务端数据库；<br>2.定时备份：默认10分钟可自定义间隔，统一定时命名规则；<br>3.一键恢复：备份历史面板选择记录，覆盖恢复当前版本；<br>4.备份管理：列表展示时间/类型/名称，支持恢复、仅标识废弃不物理删除；<br>5.本期暂不实现增量备份 |
